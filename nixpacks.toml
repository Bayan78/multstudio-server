# Railway builds with Nixpacks. ffmpeg is needed by moviepy; dejavu_fonts gives
# Cyrillic/Turkish glyphs for burned-in captions.
[phases.setup]
nixPkgs = ["python311", "ffmpeg", "dejavu_fonts"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
